[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "snake_case"
version = "0.1.0"
description = "Autonomous snake clicker game"
authors = [{name = "Unknown"}]
requires-python = ">=3.8"
dependencies = [
    "pygame==2.5.2",
    "platformdirs",
]
